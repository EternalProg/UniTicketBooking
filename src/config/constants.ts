export const API_PREFIX = "/api";

export const SWAGGER_PATH = "/docs";

export const ROLES = {
  USER: "USER",
  ADMIN: "ADMIN",
} as const;

export const BOOKING_STATUS = {
  CONFIRMED: "CONFIRMED",
  CANCELLED: "CANCELLED",
} as const;
